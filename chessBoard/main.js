function sleep(ms) {
    return new Promise(resolve => setTimeout(resolve, ms));
}


var lastCords;



// for (let i = 0; i < 100; i++) {

//     document.write("<script>")
//     document.write("var board" + i + "= new Board('board" + i + "'); \n")
//     document.write("creatHTMLboardForBoard('board" + i + "'); \n")
//     document.write("PrintBoard(board" + i + ", true); \n")
//     document.write("</script>")
// }

//playRrandomovesToBoards("board", "", 1);



//-----------------------------------------------------------------////-----------------------------------------------------------------////-----------------------------------------------------------------//
//-----------------------------------------------------------------////-----------------------------------------------------------------////-----------------------------------------------------------------//
//-----------------------------------------------------------------////-----------------------------------------------------------------////-----------------------------------------------------------------//
//-----------------------------------------------------------------////-----------------------------------------------------------------////-----------------------------------------------------------------//
//-----------------------------------------------------------------////-----------------------------------------------------------------////-----------------------------------------------------------------//

let bestNetwork = new NeuralNetwork([320, 1000, 2500, 1000, 128]);


function train(n, learnRate) {
    for (n; n > 0; n--) {
        bestNetwork = networkGame(mainBoard, bestNetwork, NeuralNetwork.mutate(bestNetwork, learnRate))
    }
}